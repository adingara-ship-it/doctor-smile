---
---

<section class="technology">
  <div class="container">
    <div class="tech-grid">
      <div class="tech-card tech-card-large reveal">
        <img src="/images/scanner-3d.jpg" alt="State-of-the-art 3D dental scanner" class="tech-img" loading="lazy" />
        <div class="tech-overlay">
          <div class="tech-tag">Technology</div>
          <h3>3D Digital Scanning</h3>
          <p>Precision diagnostics with the latest imaging technology for accurate treatment planning.</p>
        </div>
      </div>
      <div class="tech-card reveal reveal-delay-2">
        <img src="/images/team-action.jpg" alt="Doctor Smile team performing dental procedure" class="tech-img" loading="lazy" />
        <div class="tech-overlay">
          <div class="tech-tag">Our Team</div>
          <h3>Expert Care</h3>
          <p>A dedicated team working together for your perfect smile.</p>
        </div>
      </div>
      <div class="tech-card reveal reveal-delay-3">
        <img src="/images/hero-mobile.jpg" alt="Modern dental treatment room" class="tech-img" loading="lazy" />
        <div class="tech-overlay">
          <div class="tech-tag">Facility</div>
          <h3>Modern Spaces</h3>
          <p>Designed for comfort and equipped with cutting-edge technology.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .technology {
    padding: 0 0 var(--section-padding);
  }

  .tech-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr 1fr;
    gap: 20px;
    height: 400px;
  }

  .tech-card {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    cursor: default;
  }

  .tech-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out-expo);
  }

  .tech-card:hover .tech-img {
    transform: scale(1.05);
  }

  .tech-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(15, 23, 42, 0.85) 0%, rgba(15, 23, 42, 0.1) 60%, transparent 100%);
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 32px;
    transition: background 0.4s;
  }

  .tech-card:hover .tech-overlay {
    background: linear-gradient(to top, rgba(15, 23, 42, 0.9) 0%, rgba(15, 23, 42, 0.2) 60%, transparent 100%);
  }

  .tech-tag {
    display: inline-block;
    padding: 4px 12px;
    background: rgba(37, 99, 235, 0.8);
    border-radius: 6px;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: white;
    margin-bottom: 12px;
    width: fit-content;
  }

  .tech-overlay h3 {
    font-size: 1.25rem;
    color: white;
    margin-bottom: 6px;
    letter-spacing: -0.01em;
  }

  .tech-overlay p {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.5;
    max-width: 280px;
  }

  @media (max-width: 900px) {
    .tech-grid {
      grid-template-columns: 1fr;
      height: auto;
    }

    .tech-card {
      height: 280px;
    }
  }
</style>
